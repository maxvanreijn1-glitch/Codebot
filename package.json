{
  "name": "codebot",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "cd client && yarn && yarn build && cd ../server && yarn && yarn build",
    "start": "node server/dist/index.js"
  }
}
